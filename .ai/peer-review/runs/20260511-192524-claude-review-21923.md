# Peer Agent Review

| Field | Value |
|---|---|
| Target | claude |
| Mode | review |
| Project | AIZen |
| Repo | /Users/zenkim_office/Project/AIZen |
| Git repo | yes |
| Branch | main |
| Created | 2026-05-11 19:27:58 KST |
| Exit code | 0 |
| Timeout seconds | 2700 |
| Attempts | 1 |

## Request

AIZen stock-trading implementation peer gate. Review current uncommitted changes against: broker-independent core first, Alpaca paper adapter validation, KIS contract/stub only, live trading/endpoint/use forbidden without separate user approval, TypeScript existing patterns, Keychain-only secrets, no src/trading, no configs/trading, no .env.example, no secret/account values/tokens in files/logs/responses. Pay special attention to worker-04 prior NO-GO: .aizen-cache/trading state/checkpoint must not persist account values, account identifiers, order identifiers, quantities, market values, or raw broker payloads. Also verify tests cover account, quote, order lifecycle, positions, fills, registry dispatch, state redaction, KIS live-blocked stub, and risk defaults.

## Context

### Git Status

```
 M core/error/index.ts
 M core/secrets/check.ts
 M core/secrets/keychain.ts
 M core/types.ts
?? .ai/codex-hyphen-trigger-guard.json
?? .ai/peer-review/runs/20260511-170920-claude-review-62414.md
?? .ai/peer-review/runs/20260511-171220-claude-plan-65226.md
?? .ai/peer-review/runs/20260511-173805-claude-plan-24408.md
?? .ai/peer-review/runs/20260511-175010-claude-review-47101.md
?? .ai/peer-review/runs/20260511-175342-claude-plan-66826.md
?? .ai/peer-review/runs/20260511-180022-claude-review-72592.md
?? .ai/peer-review/runs/20260511-180229-claude-plan-75073.md
?? .ai/peer-review/runs/20260511-181055-claude-plan-77418.md
?? .ai/peer-review/runs/20260511-181720-claude-plan-80358.md
?? .ai/peer-review/runs/20260511-182837-claude-plan-3222.md
?? .ai/peer-review/runs/20260511-183253-claude-plan-4039.md
?? .ai/pipeline/runs/20260511-170810_stock-trading-automation-agent/
?? .ai/pipeline/runs/20260511-174922_stock-trading-plan-sequence-update/
?? .ai/pipeline/runs/20260511-175921_stock-trading-agent-implementation/
?? docs/trading/
?? skills/zen-trading-broker-alpaca/
?? skills/zen-trading-broker-kis/
?? skills/zen-trading-core/
```

### Diff Stat

```
 core/error/index.ts      |  57 ++++++++++++
 core/secrets/check.ts    |   5 ++
 core/secrets/keychain.ts |   5 ++
 core/types.ts            | 230 +++++++++++++++++++++++++++++++++++++++++++++++
 4 files changed, 297 insertions(+)
```

## Peer Result

I have enough context to render a verdict. The state/checkpoint path (worker-04 NO-GO scope) is well covered, but I want to flag one defense-in-depth gap in the audit event redaction.

### Peer Review Verdict
- Result: PASS
- Confidence: medium

### Findings
| Severity | File/Area | Issue | Recommendation |
|---|---|---|---|
| Medium | `skills/zen-trading-core/handler.ts:275-292` (`redactSensitive`) and `skills/zen-trading-core/tests/risk-policy.test.ts:129-150` | Audit event redaction is a denylist on key names (`/secret\|token\|password\|api[_-]?key\|account[_-]?id/i`). It does NOT redact worker-04 NO-GO categories when they arrive under their natural Alpaca/KIS field names (`id`, `order_id`, `client_order_id`, `qty`, `filled_qty`, `equity`, `cash`, `buying_power`, `market_value`, `avg_entry_price`, `unrealized_pl`, raw broker payloads). The test only asserts on `apiKey`/`accessToken`. By contrast, `sanitizePaperState` correctly uses an allow-list. Today no caller writes broker responses to `appendAuditEvent`, but the asymmetry is a real exposure waiting for the first audit caller. | Switch audit payload sanitization to an allow-list (or explicit shape per `type`), and extend the test to assert raw broker fields (`id`, `qty`, `equity`, `cash`, `market_value`) and a nested raw response payload are not present in `events.jsonl`. |
| Low | `skills/zen-trading-broker-alpaca/handler.ts:133-156` | `placeOrder` returns `raw: order` (full Alpaca response) on `PlacedOrder`/`OrderState`. Safe today because `sanitizePaperState` drops `raw` and audit events are not auto-emitted, but any logger/channel that JSON-stringifies the result will leak account/order identifiers. | Either drop `raw` from the typed return surface or add a dedicated `redactRawBrokerResponse` helper invoked at the adapter boundary. |
| Low | `skills/zen-trading-broker-alpaca/handler.ts:194` | `unrealized_plpc` is multiplied by 100 in `listPositions`, and the test asserts the float `0.7000000000000001`. Functionally correct, but the brittle float assertion will break if anyone changes mapping precision. | Use `assert.ok(Math.abs(positions[0].unrealizedPnlPercent - 0.7) < 1e-9)` to make the test less brittle. |
| Low | `skills/zen-trading-broker-kis/handler.ts:124-134` | `notConnected<T>(): Promise<T>` synchronously throws inside an `async` method. Works (becomes a rejected promise) but the `Promise<T>` return type is a lie — callers receive nothing. | Either annotate as `Promise<never>` or return `Promise.reject(new CoreError(...))` to keep the type honest. |
| Low | `skills/zen-trading-core/handler.ts:281` | `JSON.parse(JSON.stringify(event))` strips `undefined`, silently drops functions, and throws on circular refs. No current caller hits this, but a raw `fetch` `Response` or class instance could surprise. | Acceptable for now; consider `structuredClone` if Node ≥18 is guaranteed. |

### Test And Acceptance Notes
- Risk defaults, approve/reject paths, registry dispatch, paper endpoint pinning, account/quote/order-lifecycle/positions/fills mapping, KIS symbol normalization, KIS live block, KIS network-disabled stub, and state/checkpoint sanitization are all covered.
- State redaction test explicitly verifies `equity|cash|buyingPower|quantity|marketValue|brokerOrderId|clientOrderId|raw|account-1` are absent from `paper-state.json` — fully meets the worker-04 NO-GO for state/checkpoint.
- **Missing**: audit-log redaction test for worker-04 NO-GO categories (raw broker payload, `id`, `qty`, `equity`, `cash`, `market_value`). Only secret-key-named fields are asserted today.
- Forbidden paths confirmed absent: no `src/trading`, no `configs/trading`, no `.env.example` (verified by `find` at depth 3).
- Keychain-only secrets confirmed: `SECRETS.ALPACA_PAPER_API_KEY/SECRET`, `KIS_APP_KEY/SECRET/ACCOUNT_ID` added to `core/secrets/keychain.ts`; Alpaca adapter reads via `readSecret`; no fallbacks to env or files.
- Live blocks confirmed in three independent places: `ensurePaperOrSandbox` (core/error), `assertEnvironment` (core registry), and Alpaca `urlOrigin` baseUrl/dataBaseUrl pin.
- `.aizen-cache/trading` directory does not currently exist on disk — no stale persisted state to leak.

### Follow-Up For Requesting Agent
1. **Tighten audit event redaction to an allow-list** (or strict per-`type` shape) so `appendAuditEvent` cannot persist `raw` broker payloads, order/account identifiers, quantities, or market values even if a future caller passes them through. Mirror the rigor of `sanitizePaperState`.
2. **Extend the audit test** in `skills/zen-trading-core/tests/risk-policy.test.ts` to cover a `broker_response` event whose payload contains the raw Alpaca shape (`{ id, qty, equity, cash, market_value, ... }`) and assert none of those values land in `events.jsonl`. This is the missing half of the worker-04 NO-GO coverage.
3. Optional: drop or seal the `raw` field from `PlacedOrder`/`OrderState` to remove the latent leak surface entirely.
4. Optional: tighten KIS `notConnected` return type to `Promise<never>` and loosen the floating-point assertion in the Alpaca positions test.

No live trading / endpoint / use approval is granted — that gate remains explicitly closed.
